function calculo(soma){
    var reais = 73;
    var centavos = 39;
    var soma = reais + centavos;
    console.log(soma);
}
calculo();

function calculo2(subtração){
    var reais2 = 73;
    var centavos2 = 39;
    var subtração = reais2 - centavos2;
    console.log(subtração);
}
calculo2();